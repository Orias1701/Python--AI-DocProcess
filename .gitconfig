[core]
    autocrlf = true
    safecrlf = warn
[merge]
    conflictstyle = diff3
[credential]
    helper = manager
[init]
    defaultBranch = main
[filter "lfs"]
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
